# app_tourism

Webapp d'accueil pour une application de tourisme.

## Ouvrir le projet

Pour garder `collaborateurs`, `vehicules` et `factures` meme si le cache navigateur est vide,
lancez le serveur applicatif :

```powershell
py -3 app_server.py
```

Puis ouvrez `http://127.0.0.1:4173/`.
