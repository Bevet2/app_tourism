{
  "name": "app_tourism",
  "version": "1.0.0",
  "private": true,
  "description": "Front statique et API PostgreSQL pour l'application de tourisme.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check operations.js && node --check google-maps-loader.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
